# Git + GitHub + РґРµРїР»РѕР№ Р±РµР· Р±РѕР»Рё

РќРёР¶Рµ СЃР°РјС‹Р№ РїСЂР°РєС‚РёС‡РЅС‹Р№ РїСѓС‚СЊ, РµСЃР»Рё РІС‹ С…РѕС‚РёС‚Рµ РїСЂРѕСЃС‚Рѕ РІРЅРѕСЃРёС‚СЊ РїСЂР°РІРєРё, РґРµР»Р°С‚СЊ `git push` Рё РІС‹РєР°С‚С‹РІР°С‚СЊ РѕР±РЅРѕРІР»РµРЅРёСЏ.

## 1. РџСЂРёРІРµРґРёС‚Рµ С‚РµРєСѓС‰РёР№ СЂРµРїРѕР·РёС‚РѕСЂРёР№ РІ РїРѕСЂСЏРґРѕРє

РЈ РІР°СЃ СѓР¶Рµ РµСЃС‚СЊ git-СЂРµРїРѕР·РёС‚РѕСЂРёР№, РЅРѕ С‚РµРєСѓС‰РёР№ remote РІС‹РіР»СЏРґРёС‚ РєР°Рє Р»РѕРєР°Р»СЊРЅРѕРµ РёРјСЏ `deutschgram`, Р° РЅРµ РєР°Рє URL GitHub.

РџСЂРѕРІРµСЂСЊС‚Рµ:

```powershell
git remote -v
```

Р•СЃР»Рё С‚Р°Рј РЅРµ GitHub URL, С‚Рѕ Р»СѓС‡С€Рµ Р·Р°РјРµРЅРёС‚СЊ remote:

```powershell
git remote remove deutschgram
```

РџРѕС‚РѕРј РґРѕР±Р°РІСЊС‚Рµ РЅРѕСЂРјР°Р»СЊРЅС‹Р№ remote РЅР° GitHub:

```powershell
git remote add origin https://github.com/<your-name>/deutschgram.git
```

Р•СЃР»Рё С…РѕС‚РёС‚Рµ РёСЃРїРѕР»СЊР·РѕРІР°С‚СЊ SSH:

```powershell
git remote add origin git@github.com:<your-name>/deutschgram.git
```

## 2. РќРµ С…СЂР°РЅРёС‚Рµ СЃРµРєСЂРµС‚С‹ РІ git

Р’ СЂРµРїРѕР·РёС‚РѕСЂРёР№ РЅРµ РґРѕР»Р¶РЅС‹ РїРѕРїР°РґР°С‚СЊ:

- `.env`
- `config.local.php`
- Р»РѕРєР°Р»СЊРЅР°СЏ SQLite Р±Р°Р·Р°

Р­С‚Рѕ СѓР¶Рµ РґРѕР±Р°РІР»РµРЅРѕ РІ `.gitignore`.

## 3. РџРµСЂРІС‹Р№ РїСѓС€ РЅР° GitHub

Р•СЃР»Рё С…РѕС‚РёС‚Рµ РїРµСЂРµР№С‚Рё СЃ `master` РЅР° `main`:

```powershell
git branch -M main
```

РџРѕС‚РѕРј:

```powershell
git add .
git commit -m "Prepare MySQL deployment and IONOS docs"
git push -u origin main
```

## 4. РљР°Рє РґР°Р»СЊС€Рµ СЂР°Р±РѕС‚Р°С‚СЊ РєР°Р¶РґС‹Р№ РґРµРЅСЊ

РћР±С‹С‡РЅС‹Р№ С†РёРєР» С‚Р°РєРѕР№:

```powershell
git status
git add .
git commit -m "Short description of changes"
git push
```

## 5. РљР°Рє РІС‹РєР°С‚С‹РІР°С‚СЊ РїСЂР°РІРєРё РЅР° IONOS

### Р›СѓС‡С€РёР№ РІР°СЂРёР°РЅС‚

РСЃРїРѕР»СЊР·РѕРІР°С‚СЊ `IONOS Deploy Now`.

РўРѕРіРґР° СЃС…РµРјР° Р±СѓРґРµС‚ С‚Р°РєРѕР№:

1. Р’С‹ РїСѓС€РёС‚Рµ РєРѕРґ РІ GitHub.
2. IONOS Deploy Now Р·Р°Р±РёСЂР°РµС‚ РёР·РјРµРЅРµРЅРёСЏ СЃР°Рј.
3. Р—Р°РїСѓСЃРєР°РµС‚СЃСЏ РґРµРїР»РѕР№.
4. РџРѕРґРґРѕРјРµРЅ РѕР±РЅРѕРІР»СЏРµС‚СЃСЏ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё.

Р­С‚Рѕ Рё РµСЃС‚СЊ РІР°СЂРёР°РЅС‚ "СЏ РїСЂРѕСЃС‚Рѕ Р·Р°Р»РёРІР°СЋ РїСЂР°РІРєРё".

## 6. Р•СЃР»Рё РІС‹ РѕСЃС‚Р°РЅРµС‚РµСЃСЊ РЅР° РѕР±С‹С‡РЅРѕРј Web Hosting

РўРѕРіРґР° GitHub РѕСЃС‚Р°С‘С‚СЃСЏ РёСЃС‚РѕС‡РЅРёРєРѕРј РєРѕРґР°, РЅРѕ СЃР°Рј IONOS Web Hosting РЅРµ РґР°С‘С‚ С‚Р°РєРѕР№ Р¶Рµ РЅР°С‚РёРІРЅС‹Р№ git push-to-deploy РїРѕС‚РѕРє, РєР°Рє Deploy Now.

Р’ СЌС‚РѕРј СЃР»СѓС‡Р°Рµ Р±СѓРґРµС‚ РѕРґРЅР° РёР· СЃС…РµРј:

### РЎС…РµРјР° A

- РєРѕРґ С…СЂР°РЅРёС‚СЃСЏ РІ GitHub
- РІС‹ РїРѕСЃР»Рµ `git push` РґРѕРїРѕР»РЅРёС‚РµР»СЊРЅРѕ Р·Р°РіСЂСѓР¶Р°РµС‚Рµ С„Р°Р№Р»С‹ РїРѕ SFTP/FTPS

### РЎС…РµРјР° B

- РєРѕРґ С…СЂР°РЅРёС‚СЃСЏ РІ GitHub
- GitHub Actions РїРѕСЃР»Рµ `git push` РѕС‚РїСЂР°РІР»СЏРµС‚ С„Р°Р№Р»С‹ РЅР° IONOS Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё

Р•СЃР»Рё Р·Р°С…РѕС‚РёС‚Рµ, СЃР»РµРґСѓСЋС‰РёРј С€Р°РіРѕРј СЏ РјРѕРіСѓ РїРѕРґРіРѕС‚РѕРІРёС‚СЊ РІР°Рј Рё РІС‚РѕСЂРѕР№ РІР°СЂРёР°РЅС‚ С‚РѕР¶Рµ: GitHub Actions РґР»СЏ Р°РІС‚Рѕ-РґРµРїР»РѕСЏ РЅР° РѕР±С‹С‡РЅС‹Р№ IONOS С…РѕСЃС‚РёРЅРі.

## 7. Р§С‚Рѕ СЂРµРєРѕРјРµРЅРґСѓСЋ РёРјРµРЅРЅРѕ РІР°Рј

Р”Р»СЏ РІР°С€РµРіРѕ СЃР»СѓС‡Р°СЏ СЃР°РјС‹Р№ СѓРґРѕР±РЅС‹Р№ РїСѓС‚СЊ С‚Р°РєРѕР№:

1. РџРµСЂРµРІРµСЃС‚Рё РїСЂРѕРµРєС‚ РЅР° GitHub.
2. РџРѕРґРєР»СЋС‡РёС‚СЊ РµРіРѕ Рє IONOS Deploy Now.
3. РСЃРїРѕР»СЊР·РѕРІР°С‚СЊ MariaDB РЅР° IONOS.
4. РџРѕРґРєР»СЋС‡РёС‚СЊ РїРѕРґРґРѕРјРµРЅ `deutschgram.metro-gatecontrol.de`.
5. Р”Р°Р»СЊС€Рµ РѕР±РЅРѕРІР»СЏС‚СЊ РїСЂРѕРµРєС‚ РѕР±С‹С‡РЅС‹Рј `git push`.
